<h1><%= @post.title %></h1>

 <div class="row">
   <div class="col-md-8">
     <p><%= @post.body %></p>
   </div>
 </div>
 <br>
 <div class="row">
   <div class="col-md-8">
     <%= link_to "Edit", edit_post_path(@post), class: 'btn btn-success' %>
     <%= link_to "Delete Post", @post, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this post?'} %>
     <%= link_to "Back to Posts", posts_path, class: 'btn btn-primary pull-right' %>
   </div>
 </div>
